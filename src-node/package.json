{
  "name": "src-node",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "node __tests__/test-suite.js",
    "server": "node server.js",
    "dev": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "//overrides": "express 4 pins qs to ~6.15.1, but the array-limit bypass and the isBuffer denial of service are only fixed in 6.16.0. The change is API-compatible, so the pin is conservatism rather than a real incompatibility.",
  "overrides": {
    "qs": "^6.16.0"
  },
  "dependencies": {
    "@microsoft/microsoft-graph-client": "^3.0.7",
    "axios": "^1.20.0",
    "better-sqlite3": "^13.0.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^8.7.0"
  }
}
